/* Dynamic memory allocation: functions calloc and realloc in stdlib.h
    
    void *calloc(size_t nmemb, size_t size);
    Note// arguments are: number of elements and the size or each element size. 

    calloc - clears the memory is allocates 
    malloc - dose not clears the memory is allocates
    realloc - changes the  size of an object allocated gy provious call to mallo,
    calloc and realloc.

    void *realloc(void *ptr, size_t size);
    Note// arguments are: pointer to the original object and the new size of object.
*/

