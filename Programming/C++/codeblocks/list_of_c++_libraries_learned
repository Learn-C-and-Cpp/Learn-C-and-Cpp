					***********************************
					* HEADER FILES LEARNED 12.02.2021 * 
					***********************************

#incleude <cmath> //for sqrt(4) etc
#inlude <>
#include <limits> // for std::numeric_limits e.g std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
#include <cstdint> // for fixed width integer type e.g std::int16_t x2{};
#inclued <cassert> //  for assert() validate function parameters 

  *************
  * some notes*
  *************
  Standard enum is not type same: use enum class
  
